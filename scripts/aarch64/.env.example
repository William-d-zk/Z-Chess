# Z-Chess Docker Compose 环境变量配置模板 (ARM64)
# 复制此文件为 .env 并设置实际值
# cp .env.example .env

# PostgreSQL 数据库密码 (必需)
# 生成强密码: openssl rand -base64 32
POSTGRES_PASSWORD=

# 可选: 数据库用户和名称
# POSTGRES_USER=chess
# POSTGRES_DB=isahl_9.x
